---
description: 主项目不做正式修改与提交；调试用临时改动须在完成后恢复
alwaysApply: true
---

# 主项目修改与提交规范

- **禁止**在主项目（本仓库根目录及 `src/`、`extensions/` 等核心代码）进行任何**正式**代码修改和提交。
- **允许**在测试、调试时对主项目做**临时**修改（如加日志、改配置、断点等）。
- 调试结束后**必须**恢复主项目：撤销或还原所有临时改动，不保留未还原的修改，不提交到主项目。

## 操作示例

- ❌ 不可：在主项目新增功能、修 bug 后直接 commit/push。
- ❌ 不可：调试完后留下 `console.log`、临时配置或注释掉的代码。
- ✅ 可以：在子项目/子模块（如 `openclaw-remote`）内正常开发、提交。
- ✅ 可以：主项目临时改完 → 验证通过 → 用 `git checkout -- .` 或 `git restore` 恢复，再继续工作。

## 需要提交时

若改动确实应落在主项目，请先与用户确认后再执行提交；未经确认不要对主项目执行 `git add`/`git commit`/`git push`。
